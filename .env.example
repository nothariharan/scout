# Add real values only to an untracked .env file.
# Do not put call recordings, API keys, or user documents in source control.

ELEVENLABS_API_KEY=
ELEVENLABS_INTAKE_AGENT_ID=
ELEVENLABS_NEGOTIATOR_AGENT_ID=
# Optional: Zara-backed negotiator selected when metadata.caller_gender is female.
ELEVENLABS_NEGOTIATOR_FEMALE_AGENT_ID=
ELEVENLABS_AGENT_PHONE_NUMBER_ID=
# Keep false until a consented test destination is ready.
OUTBOUND_CALLS_ENABLED=false
# Set true only where all participants have been informed and local recording
# consent requirements are satisfied.
CALL_RECORDING_ENABLED=false
# Public OSM endpoints are suitable for user-triggered, low-volume discovery.
# Set an identifiable contact URL/email before a public deployment.
OSM_USER_AGENT=ScoutBuyingAgent/0.1 (+https://github.com/nothariharan/scout)
# Local development only; production must provide a database-backed store.
SCOUT_LOCAL_STATE_PATH=data/local/scout-state.json
MAPS_API_KEY=
DATABASE_URL=

# Backend (orchestrator) — optional; pipeline runs without these via fallbacks.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
TAVILY_API_KEY=
GOOGLE_PLACES_API_KEY=
PORT=8787

# Web server-to-server proxy. Keep this private: no NEXT_PUBLIC_ prefix.
ORCHESTRATOR_URL=http://127.0.0.1:8787
WEB_ORIGIN=http://localhost:3000

# Required only after the orchestrator is deployed publicly and the live
# ElevenLabs agent tools are provisioned. Never use NEXT_PUBLIC_ for either.
SCOUT_PUBLIC_ORCHESTRATOR_URL=
SCOUT_AGENT_TOOL_SECRET=

